# Figma progress

Файл: `7CasuBb4xJjm0UYTOGZElV`

## Concept A — Desktop

- экран: `46:2`;
- полностью собран и визуально проверен;
- Footer: секция `46:13`, композиция `60:90`, social icons `60:97`;
- Footer имеет высоту 176 px, смысловой тезис слева, две контактные колонки справа и copyright по центру.

## Новые пользовательские потоки

- Публичные данные автора синхронизированы во всех Desktop/Tablet/Mobile × Light/Dark вариантах: Header, Profile и copyright Footer используют «Полисмакова Светлана»; Footer показывает `+7 993 335-37-75` и `svetlana@polismakova.ru`. Рендеры Mobile Light и Mobile Dark подтвердили двухстрочное имя в Header без пересечения с меню, а также читаемые Profile и Footer.
- Навигация Dark Desktop (`263:209`) приведена к текстовому виду: у всех четырёх ссылок удалены fill и stroke; «Проекты» — active по статическому светлому `#F4F1EA` и акцентному подчёркиванию 2 px `Navigation active underline / Dark / Desktop` (`508:413`, sage/300 `#B7C2AE`), остальные используют secondary text. В Light добавлено симметричное подчёркивание `Navigation active underline / Light / Desktop` (`514:412`, sage/500 `#6F7868`). После исправления оба line расположены на `y=151`: Header-section начинается на `y=101`, локальная линия стоит на 50 px ниже — непосредственно под активным текстом. Статический цвет исключает недостаточный контраст от token inheritance внутри экземпляра Header. Header-рендеры Desktop/Tablet/Mobile в Light/Dark подтверждают отсутствие наложений. На Tablet/Mobile базовый Header содержит прозрачный контрол «Меню», поэтому отдельный active-link появится вместе с будущим раскрытым меню.
- Hero‑карусель оформлена как три reusable component set: Desktop `Hero Carousel / Viewport=Desktop` (`525:432`, 616 × 632 px), Tablet `Hero Carousel / Viewport=Tablet` (`535:687`, 720 × 436 px) и Mobile `Hero Carousel / Viewport=Mobile` (`535:1348`, 342 × 360 px). Каждый содержит 30 кадров: 01–10 — «Современная гармония», 11–20 — «Атмосфера уюта», 21–30 — «Современный минимализм», с `FILL`, preview-filmstrip, счётчиком и стрелками. Все variants имеют `AFTER_TIMEOUT` 3 000 ms, `CHANGE_TO`, dissolve 300 ms и loop `Frame=30 → Frame=01`; ручные стрелки также замкнуты. Во всех шести базовых Hero стоят instances: Desktop Light/Dark `526:412`/`526:413`, Tablet Light/Dark `535:1439`/`535:1458`, Mobile Light/Dark `535:1477`/`535:1496`. Визуальные рендеры Tablet и Mobile Light/Dark подтвердили читаемость controls и отсутствие искажений.
- Исследование замены фото автора: рядом с Desktop Light Profile создан frame `Author media alternatives / Desktop / Light` (`496:412`) с пятью подписанными направлениями: план и подпись, материальная палитра, архитектурный макет, свет и объём, монограмма «ПС». Рендер 1440 × 402 px подтверждает читаемость подписей и контраст; исходный портрет сохранён для сравнения.

- Ссылка «Подробнее об авторе →» удалена из Profile во всех шести вариантах Concept A: Desktop/Tablet/Mobile × Light/Dark. Ссылка «Все проекты →» в Featured Projects сохранена.
- Форма обратной связи: отдельный экран `Contact Form / Desktop / Light` (`433:437`) с затемнённым фоном, полями «Имя», «Телефон или почта», «Тип проекта», «О проекте», согласием на обработку данных, отправкой и действием «Закрыть».
- Прототип: CTA «Обсудить проект» (`427:398`) по клику с dissolve-переходом открывает форму как overlay; «Закрыть» (`433:461`) закрывает overlay.
- Адаптивная матрица формы завершена: Desktop Dark `438:525`, Tablet Light `438:549`, Tablet Dark `438:573`, Mobile Light `438:597`, Mobile Dark `438:621`; исходный Desktop Light — `433:437`.
- Структурная проверка prototype-связей подтверждает, что каждый CTA ведёт к форме той же темы и viewport: `427:398 → 433:437`, `I263:254;36:10 → 438:525`, `I168:135;36:31 → 438:549`, `I291:320;36:31 → 438:573`, `I185:187;36:52 → 438:597`, `I326:404;36:52 → 438:621`. Все шесть «Закрыть» настроены как `CLOSE`; ручной прогон в режиме Figma Prototype остаётся финальной проверкой перед архивированием change.
- Текстовые действия «Закрыть» удалены. Во всех шести dialog добавлен `form-close`: иконка `×` с интерактивной областью `44 × 44 px`, `color/text/secondary`, действием `CLOSE` и безопасным отступом справа — 48 px на Desktop/Tablet и 24 px на Mobile.
- После удаления текстового действия высота dialog сокращена до 810 px на Desktop/Tablet и 814 px на Mobile. Рендерами подтверждены компактные нижние отступы под CTA: 48 px и 24 px соответственно, без обрезания содержимого.
- Контейнеры `field-*` сделаны прозрачными, а заливка `color/bg/page` перенесена на дочерние `input`. Рендеры Light/Dark подтверждают: label больше не имеют отдельной фоновой плашки, область ввода остаётся различимой по заливке и border.
- В Dark на формах применён явный режим `Color / Dark`; поверхность полей связана с `color/bg/page`. Mobile-формы `390 × 1000 px` имеют вертикальную прокрутку, внутренние отступы 24 px и проверенный двухстрочный placeholder типа проекта.
- Рендеры Desktop/Tablet/Mobile в Light/Dark проверены: нет горизонтального overflow, обрезанных текстов, пересечений и светлых поверхностей в Dark-вариантах.
- Каталог: экран `All Projects / Desktop / Light` (`433:462`), `1440 × 1240 px`; включает Header, вводный блок и галерею 3 × 2 из шести project cards с фото, названием и контекстным описанием (тип объекта, география, год).
- Прототип: «Все проекты →» (`51:29`) в Featured Projects открывает каталог с dissolve-переходом.
- Реальные case studies (Desktop Light): `Project / Современная гармония / Desktop / Light` (`453:488`), `Project / Атмосфера уюта / Desktop / Light` (`453:489`) и `Project / Современный минимализм / Desktop / Light` (`453:490`). Каждый экран использует исходный текст из соответствующего `.docx`, главную фотографию, 10 видимых фотографий слайдов, счётчик `01 / 10` и кнопки-стрелки 48 × 48 px. Для передачи в разработку фотонаборы разнесены в отдельные именованные frames: `461:474`, `461:495`, `461:516`; в каждом ровно 10 кадров одного проекта. Первые три карточки каталога заменены на реальные проекты и связаны с их экранами переходом `NAVIGATE`; демонстрационные карточки второго ряда удалены, высота каталога сокращена до 760 px.
- Для этих же кейсов добавлены Desktop Dark-варианты: `455:447`, `455:473`, `455:491`; корневые frames используют явный режим `Color / Dark` существующей коллекции токенов.
- Блоки Featured Projects обновлены во всех шести макетах: заголовок — «Последние проекты», отдельная Text Link «Все проекты →» удалена. На каждой карточке добавлена круглая иконка `i` с именем `Подробнее — информация`, интерактивной областью 44 × 44 px и тем же prototype-переходом, что у карточки. На Tablet Light (`72:89`, `76:93`, `83:97`) и Tablet Dark (`291:293`, `291:294`, `291:295`) высота каждой карточки установлена в 356 px; визуальный рендер Tablet Light подтверждает отсутствие наложений и корректное расположение иконок.
- Для всех трёх кейсов добавлена единая адаптивная матрица case-study. Tablet Light: `468:446`, `468:534`, `468:622`; Tablet Dark: `468:468`, `468:556`, `468:644`; Mobile Light: `468:490`, `468:578`, `468:666`; Mobile Dark: `468:512`, `468:600`, `468:688`. Во всех вариантах сохранены breadcrumb, название, краткое описание, главное фото, 10 миниатюр, счётчик `01 / 10` и стрелки 44 × 44 px. Рендеры Tablet и Mobile Light проверены визуально; композиции не имеют наложений или обрезанных элементов.
- Возврат из кейса: на всех 18 экранах действие `Назад` отображается как компактная иконка `←` размером `44 × 44 px`, выровненная справа в одной строке с названием. Title получает безопасный зазор 16 px на Desktop/Tablet и 12 px на Mobile. Prototype возвращает на экран «Последние проекты» соответствующих viewport и темы; размеры, отсутствие пересечений и destinations структурно проверены для всех 18 действий.
- Визуальная коррекция case-study: шесть Desktop-экранов пересобраны в тот же шаблон, что и адаптивные варианты, с 10 миниатюрами и счётчиком `01 / 10`; устаревшая трёхкадровая композиция удалена. Непреднамеренные белые fills во вложенных desktop-контейнерах устранены. Light-среда приведена к `#FBFAF7`, Dark — к `#111310`; рендеры Desktop Light/Dark, Tablet Dark и Mobile Light/Dark визуально подтверждают согласованную композицию и тему.
- Footer лендинга добавлен на каждый из 18 case-study. Для Desktop копируется 176 px Footer `46:13`/`263:255`, для Tablet — 176 px `63:88`/`291:321`, для Mobile — 250 px `185:188`/`326:406`. Footer располагается full-bleed по нижней границе каждого case-study; структурная проверка подтверждает ширину, соответствующую viewport, и отсутствие пустого пространства после него.

## Concept A — Tablet

- экран: `63:77`, ширина 768 px;
- Header: секция `63:81`, instance `63:101`;
- Hero: секция `63:82`, inner `63:110`;
- готовые Header и Hero визуально проверены;
- Featured Projects: секция `63:83`, heading `87:101`, heading-row `90:101`, responsive grid `75:93`; фон связан с desktop-токеном `color/bg/surface` (`#FBFAF7`);
- ссылка «Все проекты →»: instance `101:104` из `Text Link / Viewport=Tablet`, `Usage=Inline`, размещена справа в heading-row с gap `78 px`;
- Project Card instances: `72:89`, `76:93`, `83:97`; используется связанный набор `Project Card / Viewport=Tablet`, раскладка `2 + 1`, grid `720 × 664` с gap `24 px`;
- секция `63:83` полностью собрана и визуально проверена Figma-рендером `768 × 900`: текст не обрезан, пересечений и пустых media placeholders нет;
- Services: секция `63:84`, heading `112:102`, intro `112:103`, list `116:102`; связанные `Service Item / Viewport=Tablet` instances `116:103`, `116:109`, `116:115` собраны в непрерывный список `720 × 300`;
- секция `63:84` полностью собрана и визуально проверена Figma-рендером `768 × 600`; используются `Display/Editorial`, `Body/Base` и семантические цветовые токены;
- Process: секция `63:85`, heading `127:117`, intro `130:117`, list `131:117`; связанные `Process Step / Viewport=Tablet` instances `133:117`, `136:122`, `137:127` собраны в непрерывный список `720 × 300`;
- секция `63:85` полностью собрана и визуально проверена Figma-рендером `768 × 600`; тексты трёх шагов синхронизированы с desktop-версией;
- Profile: секция `63:86`, inner `148:132`, `Media Figure / Viewport=Tablet` instance `148:133`, copy `148:134`, `Text Link / Viewport=Tablet` instance `148:139`;
- секция `63:86` полностью собрана и визуально проверена Figma-рендером `768 × 700`; используется двухколоночная сетка `336 + 24 + 360 px`, имя автора адаптировано до `48/52` без пересечения с описанием;
- Contact CTA: секция `63:87`, связанный `Contact CTA / Viewport=Tablet` instance `168:135`, ширина контента `720 px` и вертикальные отступы `80 px`;
- Footer: секция `63:88`, композиция `176:139`, main `176:140`, contacts `176:142`, social icons `176:146`; колонки адаптированы к ширине `768 px` без обрезки;
- полный экран `63:77` собран и визуально проверен Figma-рендером `768 × 4448`; пустых секций, наложений и обрезанного текста нет;
- локальный дизайн-прототип [`designs/landing/concept-a-tablet.html`](../../designs/landing/concept-a-tablet.html) остаётся reference для адаптивной композиции;
- Tablet Concept A завершён.

## Concept A — Mobile

- экран: `185:139`, ширина 390 px; создан на основе подтверждённой Tablet-композиции;
- Header: секция `185:143`, `Header / Viewport=Mobile` instance `185:144`, высота 64 px;
- Hero: секция `185:145`, inner `185:146`, copy `185:147`, mobile button `185:152`, изображение `185:154`; композиция полностью адаптирована и проверена рендером `390 × 900`;
- Featured Projects: секция `185:155`, heading `185:156`, mobile link `185:158`, вертикальный grid `185:159`; `Project Card / Viewport=Mobile` instances `185:160`, `185:161`, `185:162` собраны в одноколоночный список;
- Services: секция `185:163`, вводный текст `185:165` адаптирован до 342 px, список `185:166` собран из `Service Item / Viewport=Mobile` instances `185:167`, `185:168`, `185:169`;
- Process: секция `185:170`, заголовок `185:171` и вводный текст `185:172` адаптированы до 342 px, список `185:173` собран из `Process Step / Viewport=Mobile` instances `185:174`, `185:175`, `185:176`;
- Profile: секция `185:177` собрана вертикально; `Media Figure / Viewport=Mobile` instance `185:179`, copy `185:180`; имя `185:182` уменьшено до 40/44 px и ограничено областью 342 × 88 px, ссылка `185:185` переведена на Mobile;
- Contact CTA: секция `185:186` высотой 443 px, instance `185:187` переведён на `Contact CTA / Viewport=Mobile`, размер 342 × 283 px; под кнопкой сохранён внутренний отступ 24 px;
- Footer: секция `185:188` увеличена до 250 px; main `185:190` и contacts `185:192` адаптированы под ширину 342 px, телефон, email и социальные ссылки полностью видимы;
- секция `185:155` проверена рендером `390 × 1292`: карточки не обрезаны и не пересекаются;
- Light mobile layout завершён и проверен целиком на ширине 390 px.

## Concept A — Desktop Dark

- экран: `263:204`, ширина 1440 px; размещён непосредственно справа от Desktop Light (`x=1720`);
- корневой frame переведён в режим `Color / Dark`, вложенные компоненты дополнительно синхронизированы с Dark-режимом;
- Project Card instances `263:225`, `263:226`, `263:227`, Service Item instances `263:233`, `263:234`, `263:235` и Process Step instances `263:241`, `263:242`, `263:243` визуально приведены к единой тёмной теме;
- Header `263:209`, hero button `263:217`, Text Link instances `263:223`, `263:252`, Media Figure `263:246` и Contact CTA `263:254` проверены в контексте экрана;
- Footer `263:255` полностью переведён в Dark: фон `263:256` использует `color/bg/page`, заголовок `263:258`, телефон `263:261` и email `263:262` используют `color/text/primary`; социальные иконки и copyright сохраняют корректный контраст;
- полный экран проверен Figma-рендером `1440 × 3949`: секции не пересекаются, тексты и изображения не обрезаны, случайных Light-строк в списках нет.

Концепции B и C удалены после согласования Concept A. В desktop-ряду остаются только Concept A Light (`x=80`) и Concept A Dark (`x=1720`).

## Concept A — Tablet Dark

- экран: `291:272`, ширина 768 px; создан отдельной копией подтверждённого Tablet Light и размещён рядом с ним (`x=888`, `y=4300`);
- корневой frame и вложенные Header, Button, Text Link, Project Card, Service Item, Process Step, Media Figure и Contact CTA переведены в `Color / Dark`;
- локальные Light-остатки в карточке `291:294`, услуге `291:301` и шагах `291:307`, `291:309` устранены после полной визуальной проверки;
- заголовок проектов `291:290` получил светлый контрастный цвет `#F4F1EA`;
- Footer `291:321` полностью тёмный: frame `291:322` использует `color/bg/page`, тексты `291:324`, `291:327`, `291:328` используют светлый primary-контраст;
- полный экран проверен Figma-рендером `768 × 4448`: фон и компоненты согласованы, наложений, обрезанного текста и случайных светлых строк нет.

Порядок адаптивных экранов в нижнем ряду: Tablet Light `63:77` (`x=80`, `y=4300`), Tablet Dark `291:272` (`x=888`, `y=4300`), Mobile Light `185:139` (`x=1696`, `y=4300`), Mobile Dark `326:356` (`x=2126`, `y=4300`).

## Concept A — Mobile Dark

- экран: `326:356`, ширина 390 px; расположен непосредственно справа от Mobile Light;
- создан из подтверждённой мобильной композиции и переведён в режим `Color / Dark` на уровне корневого frame;
- вложенные Header, Button, Text Link, Project Card, Service Item, Process Step, Media Figure, Contact CTA и контейнеры Footer синхронизированы с явным режимом `Dark`; локальные Light-переопределения устранены;
- точечная проверка Dark: заголовок проектов `326:374` переведён со статического `#1E211F` на светлый контрастный цвет, карточка «Тихий двор» `326:379` синхронизирована с Dark, футер `326:406` переведён с `color/bg/inverse` на `color/bg/page`, его основные тексты — на `color/text/primary`;
- кнопка «Все проекты →» `326:375` в Dark-экране оформлена как светлый акцент: фон и обводка `#F4F1EA`, текст `#111310`; контраст проверен отдельным рендером `123 × 32` px;
- сохранены размеры и адаптивная структура Mobile Light: `390 × 5415` px.

## Следующий этап

1. Провести общую визуальную и структурную проверку Light/Dark.

Все корневые секции занимают 100% ширины viewport; ограничение контента выполняется внутренними responsive gutters.
